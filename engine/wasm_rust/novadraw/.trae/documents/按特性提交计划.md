# 按特性提交计划（稳定优先）

## 总览
将当前改动按“架构层级与特性”拆分为独立提交，保证每个提交主题单一、易于回溯。提交顺序从核心协议 → 更新/修复语义 → 后端执行 → 平台调度 → 测试支撑 → 文档与目录整理。

## Commit 1：渲染提交协议收敛（RenderSubmission/ DamageSet）
- 目的：把录制层与提交层边界分离，稳定后端输入契约。
- 文件（仅这些）：
  - novadraw-render/src/submission.rs（新增）
  - novadraw-render/src/context.rs（移出 RenderSubmission/DamageSet 依赖到 submission）
  - novadraw-render/src/lib.rs（导出 submission）
  - novadraw-render/src/traits.rs（RenderBackend::render(&RenderSubmission)）
  - novadraw/src/lib.rs（re-export 调整）
  - novadraw-apps/src/app.rs（调用点改为传 RenderSubmission）
- 提交信息：
  - feat(render): introduce RenderSubmission & DamageSet; backend now consumes submission not raw commands

## Commit 2：Update/Repair 职责收敛（repair 模块下沉）
- 目的：让 SceneUpdateManager 只管流程，repair 专管算法与写入。
- 文件：
  - novadraw-scene/src/update/repair.rs（新增）
  - novadraw-scene/src/update/mod.rs（mod repair）
  - novadraw-scene/src/update/deferred.rs（调用 repair 接口）
- 提交信息：
  - refactor(scene/update): move damage compute & write to repair module, keep deferred as orchestrator

## Commit 3：Damage Repair 语义增强（snapshot/parent-chain/regions）
- 目的：落地 g2 精神：快照 → 父链传播 → 写入 regions → union 强约束。
- 文件：
  - novadraw-scene/src/update/deferred.rs（take dirty snapshot、清理逻辑、is_update_queued）
  - novadraw-scene/src/update/repair.rs（parent-chain 传播、normalize、写入 regions、测试）
- 提交信息：
  - feat(scene/update): snapshot-based repair; parent-chain propagation; emit DamageSet.regions with union

## Commit 4：Vello 后端 region-aware 合成（union 兼容回退）
- 目的：在保持 union 裁剪的前提下，按 regions 逐块 copy；resize 后重建 retained/scratch。
- 文件：
  - novadraw-render/src/backend/vello/mod.rs
- 提交信息：
  - feat(render/vello): consume DamageSet.regions for retained copy with union clip; recreate offscreen on resize

## Commit 5：平台调度与下一轮承接
- 目的：只在空→非空时 request_redraw；repair 中新增 dirty 进入下一轮。
- 文件：
  - apps/editor/src/scene_manager/scene_host.rs（幂等 request_update、根据 is_update_queued 决定是否继续排队）
  - apps/editor/src/app_window.rs（去除高频输入上的无条件 request_update）
  - apps/editor/src/main.rs（如有必要的微调）
- 提交信息：
  - feat(editor): coalesce update requests and honor next-cycle queuing via is_update_queued

## Commit 6：editor 测试/调试支撑（可选合并在一起）
- 目的：交互验证与日志支撑，不与核心语义混提交。
- 文件：
  - apps/editor/src/system.rs（交互脚本/验证入口）
  - apps/editor/src/scene_manager/interactive_figure.rs
  - apps/editor/src/scene_manager/mod.rs
  - apps/editor/Cargo.toml（依赖或 feature 调整）
- 提交信息：
  - chore(editor): add interaction harness and test scaffolding for manual verification

## Commit 7：文档与目录整理（最后）
- 目的：保留项目演进记录，统一文档索引；避免与核心代码混在一起。
- 文件：
  - CLAUDE.md
  - doc/00-index.md
  - doc/01-architecture/*（新增）
  - doc/deprecated/*（迁移/归档）
- 提交信息：
  - docs: update ideal architecture docs and archive deprecated notes

## 注意事项
- 不要混合提交：
  - editor/* 与 render/backend/*
  - docs/* 与核心代码
  - 目录搬迁与行为语义
- 每个提交前：跑 `cargo test -p novadraw-scene update::`、`cargo check -p editor`、`cargo run -p shape-app -- --screenshot=0`。
- Commit message 用英文简洁描述“目的 + 范围”，必要时附带简短说明。

## 可执行提交顺序
1. RenderSubmission & DamageSet（协议）
2. update/repair 下沉（职责）
3. snapshot/parent-chain/regions（语义）
4. vello region-aware（执行）
5. editor 调度与承接（平台）
6. editor 测试支撑（工具）
7. 文档与目录（文档）

## 说明
上述分组基于当前 `git status` 与 `git diff --name-only` 的实际文件，尽量保证“单一主题、最小变更”。如遇文件交叉（同文件同时涉及两个主题），优先把**语义变更**归入早期提交，**格式/辅助变更**归入后续提交。
