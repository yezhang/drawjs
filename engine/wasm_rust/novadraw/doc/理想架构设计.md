# 理想架构设计

## 概述

本文档基于对 Eclipse draw2d 核心组件（Figure、LayoutManager、UpdateManager、LightweightSystem）的完整源码分析，结合 Rust 语言特性，给出 Novadraw 的理想架构设计。

设计遵循三大原则：

1. **同职责内聚**：同一职责域的数据和方法尽量内聚
2. **避免枚举多态**：使用 `dyn Trait` 实现灵活的多态扩展
3. **ID 引用避免借用**：树层次结构使用 `SlotMap<BlockId, T>` 存储，通过 ID 而非嵌套引用遍历

## draw2d 核心组件职责分析

### Figure 类职责过载问题

draw2d 中 `IFigure` 接口有 **100+ 方法**，`Figure` 实现类约 2400 行。它混合了完全不同的职责：

| 职责类别 | 具体内容 |
|---|---|
| 树结构 | `parent`, `children`, `add()`, `remove()` |
| 几何/坐标 | `bounds`, `setBounds()`, `translate()`, `containsPoint()` |
| 渲染 | `paint()`, `paintFigure()`, `paintBorder()`, `paintClientArea()` |
| 布局 | `layoutManager`, `getPreferredSize()`, `setConstraint()` |
| 验证 | `validate()`, `invalidate()`, `revalidate()` |
| 状态 | `isVisible`, `isEnabled`, `isOpaque` |
| 事件 | 10+ 种 listener 注册和 fire 方法 |
| 更新 | `repaint()`, `getUpdateManager()` |
| 样式 | `bgColor`, `fgColor`, `font`, `border` |

这正是 Java 单一继承的局限性——draw2d 不得不使用"上帝接口"把所有东西塞进去。

### LightweightSystem 的桥接职责

draw2d 中 `LightweightSystem` 是 SWT Canvas 与 Figure 树之间的桥接器，同时持有三个关键组件：

```
LightweightSystem
  ├── Canvas canvas                  // SWT 底层画布
  ├── IFigure root (RootFigure)     // Figure 树根节点
  ├── UpdateManager manager         // 更新管理器
  └── EventDispatcher dispatcher    // 事件分发器

UpdateManager
  └── IFigure root                  // 持有 root，调用 root.paint()

EventDispatcher
  ├── Canvas control
  └── IFigure root                   // 用于事件路由
```

### UpdateManager 两阶段更新模式

```
Phase 1: Validation
  addInvalidFigure(fig) → for each: fig.validate() → layout() + children.validate()

Phase 2: Damage Repair
  addDirtyRegion(fig, rect) → repairDamage()
    → 向父链传播脏区并交集
    → root.paint(graphics)
```

### LayoutManager 纯接口设计

LayoutManager 不持有任何状态，只是一个纯接口：

```java
interface LayoutManager {
    Object getConstraint(IFigure child);
    void setConstraint(IFigure child, Object constraint);
    Dimension getMinimumSize(IFigure container, int wHint, int hHint);
    Dimension getPreferredSize(IFigure container, int wHint, int hHint);
    void invalidate();
    void layout(IFigure container);
    void remove(IFigure child);
}
```

### draw2d Figure 的 paint 模板方法

```text
paint(Graphics)
  ├── 1. 设置背景色/前景色/字体
  ├── 2. pushState()
  ├── 3. paintFigure()           ← 子类覆盖：绘制背景
  ├── 4. restoreState()          ← 恢复颜色，保留变换
  ├── 5. paintClientArea()       ← 遍历子节点 child.paint()
  ├── 6. paintBorder()           ← 绘制边框
  └── 7. popState()
```

## 核心结构与 Trait 设计

### 架构总览图

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FigureGraph                                    │
│                                                                              │
│  职责：树结构管理 + 协调调度                                                │
│                                                                              │
│  # 核心数据                                                                 │
│  SlotMap<BlockId, FigureBlock>  nodes;     // 所有节点，ID 索引         │
│  HashMap<Uuid, BlockId>          uuid_map; // UUID 快速查找               │
│  BlockId                           root;     // 根节点 ID                    │
│  BlockId                           contents; // 用户可见的根容器 ID            │
│                                                                              │
│  # 协调职责                                                                 │
│  - 树遍历 → nodes SlotMap                                                │
│  - 布局计算 → LayoutManager trait                                        │
│  - 更新调度 → UpdateManager trait                                        │
│  - 渲染触发 → SceneHost                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ 持有 SlotMap<BlockId, FigureBlock>
                                  │ 节点之间通过 BlockId 引用（无嵌套借用）
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  FigureBlock (节点状态)                                                    │
│                                                                              │
│  职责：单个节点的运行时状态，与渲染无关的一切                                │
│                                                                              │
│  # 节点身份                                                               │
│  BlockId              id;          // 自身 ID                              │
│  Option<BlockId>     parent;      // 父节点 ID                           │
│  Vec<BlockId>        children;     // 子节点 ID 列表                       │
│                                                                              │
│  # 运行时状态                                                             │
│  bool                is_visible;                                           │
│  bool                is_enabled;                                           │
│  bool                is_selected;                                           │
│  bool                is_valid;           // 验证标记                         │
│                                                                              │
│  # 布局相关                                                               │
│  Arc<dyn LayoutManager> layout_manager;                                    │
│  HashMap<BlockId, Constraint> constraints;  // 子节点约束                   │
│  Option<(f64, f64)> preferred_size;                                        │
│  Option<(f64, f64)> minimum_size;                                          │
│  Option<(f64, f64)> maximum_size;                                          │
│  bool                layout_valid;                                           │
│                                                                              │
│  # 几何属性                                                               │
│  Box<dyn Figure>    figure;           // 几何定义 + 渲染指令              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### FigureGraph 持有关系图

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                              FigureGraph                                    │
│                                                                              │
│  nodes: SlotMap<BlockId, FigureBlock>                                      │
│                                                                              │
│  FigureGraph.nodes.get(id) → FigureBlock {                                 │
│      id, parent, children: Vec<BlockId>                                    │
│      is_visible, is_enabled, is_selected                                   │
│      layout_manager, constraints                                            │
│      figure: Box<dyn Figure>           ← 渲染接口在此                        │
│  }                                                                          │
│                                                                              │
│  其他组件通过 Arc<dyn Trait> 注入：                                        │
│  layout_manager: Arc<dyn LayoutManager>  ──► 单例布局器                   │
│  update_manager: Arc<dyn UpdateManager>  ──► 单例更新调度                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Arc<dyn Trait> 注入
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  LayoutManager (Trait)                                                       │
│      │                                                                      │
│      ├── FlowLayout                                                         │
│      ├── BorderLayout                                                       │
│      ├── FillLayout                                                         │
│      └── 用户自定义 impl LayoutManager  ← 任意扩展                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  UpdateManager (Trait)                                                       │
│      │                                                                      │
│      ├── SceneUpdateManager (novadraw 实现)                                 │
│      └── 用户自定义 impl UpdateManager                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│  Figure (Trait) ← dyn Figure                                               │
│      │                                                                      │
│      ├── RectangleFigure                                                    │
│      ├── EllipseFigure                                                      │
│      ├── TriangleFigure                                                     │
│      └── 用户自定义 impl Figure  ← 任意扩展                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 组件对应关系表

| draw2d 组件 | Novadraw 对应 | 说明 |
|---|---|---|
| `IFigure` / `Figure` | `dyn Figure` + `FigureBlock` | Trait 定义渲染接口，FigureBlock 管理运行时状态 |
| `Figure.parent` / `children` | `FigureBlock.parent` / `children` | 改为 ID 引用，避免嵌套借用 |
| `Figure.paint()` | `FigureGraph` 遍历调度 | 模板方法在渲染器中实现 |
| `Figure.getPreferredSize()` | `LayoutManager.get_preferred_size()` | 布局信息移到 LayoutManager |
| `LayoutManager` | `LayoutManager` trait | 保持一致，纯接口设计 |
| `UpdateManager` | `UpdateManager` trait | 保持一致，两阶段更新 |
| `LightweightSystem` | `SceneHost` | 平台桥接 + 协调调度 |
| `RootFigure` | `FigureGraph` 内置根节点 | 内部使用，用户不直接操作 |

## 核心 Trait 设计

### Figure Trait（几何 + 渲染）

```rust
/// 图形 Trait - 几何定义 + 渲染指令
///
/// 对应 draw2d: Figure.paintFigure() + Shape
///
/// 原则：Figure 不持有任何运行时状态，状态全部在 FigureBlock 中
pub trait Figure: Send + Sync {
    // === 几何域 ===
    fn bounds(&self) -> Rectangle;
    fn set_bounds(&mut self, x: f64, y: f64, width: f64, height: f64);
    fn name(&self) -> &'static str;

    fn contains_point(&self, x: f64, y: f64) -> bool {
        let b = self.bounds();
        x >= b.x && x <= b.x + b.width && y >= b.y && y <= b.y + b.height
    }

    fn intersects(&self, rect: Rectangle) -> bool { ... }
    fn insets(&self) -> (f64, f64, f64, f64) { (0.0, 0.0, 0.0, 0.0) }
    fn use_local_coordinates(&self) -> bool { false }

    fn client_area(&self) -> Rectangle {
        let b = self.bounds();
        let (top, left, bottom, right) = self.insets();
        Rectangle::new(b.x + left, b.y + top, b.width - left - right, b.height - top - bottom)
    }

    // === 渲染域 ===
    fn paint_figure(&self, gc: &mut NdCanvas);
    fn paint_border(&self, gc: &mut NdCanvas) {
        if let Some(border) = self.get_border() {
            border.paint(self.bounds(), gc);
        }
    }
    fn get_border(&self) -> Option<&dyn Border> { None }

    // === 验证域 ===
    fn validate(&mut self) {}
}

/// Shape Trait - 描边/填充
///
/// 对应 draw2d: Shape 类
pub trait Shape: Figure {
    fn fill_shape(&self, gc: &mut NdCanvas);
    fn outline_shape(&self, gc: &mut NdCanvas);
    fn stroke_color(&self) -> Option<Color>;
    fn fill_color(&self) -> Option<Color>;
    fn stroke_width(&self) -> f64 { 1.0 }
    fn line_cap(&self) -> LineCap { LineCap::Butt }
    fn line_join(&self) -> LineJoin { LineJoin::Miter }
    fn alpha(&self) -> f64 { 1.0 }
}

/// Blanket Impl：所有实现 Shape 的类型自动获得 Figure 实现
impl<T: Shape> Figure for T {
    fn paint_figure(&self, gc: &mut NdCanvas) {
        self.paint_fill(gc);
        self.paint_outline(gc);
    }
    fn get_border(&self) -> Option<&dyn Border> { Shape::get_border(self) }
}
```

### LayoutManager Trait（纯接口）

```rust
/// 布局管理器 Trait
///
/// 对应 draw2d: LayoutManager 接口
///
/// 原则：纯接口，不持有状态，通过 BlockId 查询 FigureGraph 获取节点数据
pub trait LayoutManager: Send + Sync {
    fn get_preferred_size(
        &self,
        container_id: BlockId,
        graph: &FigureGraph,
        w_hint: f64,
        h_hint: f64,
    ) -> (f64, f64);

    fn get_minimum_size(
        &self,
        container_id: BlockId,
        graph: &FigureGraph,
        w_hint: f64,
        h_hint: f64,
    ) -> (f64, f64);

    fn layout(&self, container_id: BlockId, graph: &mut FigureGraph);
    fn invalidate(&self) {}

    fn get_constraint(&self, child_id: BlockId) -> Option<&dyn Constraint>;
    fn set_constraint(&mut self, child_id: BlockId, constraint: Box<dyn Constraint>);
    fn remove(&mut self, child_id: BlockId);
}

/// 布局约束 Trait
pub trait Constraint: Send + Sync {}
```

### UpdateManager Trait（两阶段更新）

```rust
/// 更新管理器 Trait
///
/// 对应 draw2d: UpdateManager 抽象类
///
/// 两阶段更新：Phase 1 Validation + Phase 2 Damage Repair
pub trait UpdateManager: Send + Sync {
    // === 收集阶段 ===
    fn add_dirty_region(&mut self, block_id: BlockId, rect: Rectangle);
    fn add_invalid_figure(&mut self, block_id: BlockId);

    // === 执行阶段 ===
    fn perform_update(&mut self, graph: &mut FigureGraph, canvas: &mut NdCanvas);
    fn perform_validation(&mut self, graph: &mut FigureGraph);

    // === 生命周期 ===
    fn is_updating(&self) -> bool;
    fn dispose(&mut self) {}
}

/// 更新监听器 Trait
pub trait UpdateListener: Send + Sync {
    fn notify_painting(&self, damage: Rectangle, dirty_regions: &HashMap<BlockId, Rectangle>);
    fn notify_validating(&self);
}
```

## FigureBlock 职责边界

### 当前问题

当前 `FigureBlock` 混合了以下职责：

| 当前状态 | 问题 |
|---|---|
| `figure: Box<dyn Figure>` | 正确，与渲染接口绑定 |
| `preferred_size`, `minimum_size`, `maximum_size` | 应属于布局信息，在 FigureBlock 中可接受，但不应在 Figure trait 中 |
| `layout_manager: Arc<dyn LayoutManager>` | 每个容器可独立布局，正确 |
| `constraints: HashMap<BlockId, Constraint>` | 属于布局，在 FigureBlock 中正确 |
| `layout_valid` | 属于布局验证，在 FigureBlock 中正确 |
| `is_visible`, `is_enabled`, `is_selected` | 属于运行时状态，正确 |
| `set_bounds()` 方法 | 应直接操作 `figure.set_bounds()`，保持现状 |
| `get_preferred_size()` 方法 | 应委托给 `layout_manager`，不是 `figure` |

### 理想职责

```rust
pub struct FigureBlock {
    // === 节点身份 ===
    pub id: BlockId,
    pub uuid: Uuid,
    pub parent: Option<BlockId>,
    pub children: Vec<BlockId>,

    // === 运行时状态 ===
    pub is_visible: bool,
    pub is_enabled: bool,
    pub is_selected: bool,
    pub is_valid: bool,

    // === 布局状态 ===
    pub layout_manager: Option<Arc<dyn LayoutManager>>,
    pub constraints: HashMap<BlockId, Box<dyn Constraint>>,
    pub preferred_size: Option<(f64, f64)>,
    pub minimum_size: Option<(f64, f64)>,
    pub maximum_size: Option<(f64, f64)>,
    pub layout_valid: bool,

    // === 渲染接口 ===
    pub figure: Box<dyn Figure>,
}
```

### FigureBlock 方法设计

```rust
impl FigureBlock {
    // === 树管理 ===
    pub fn add_child(&mut self, child_id: BlockId);
    pub fn remove_child(&mut self, child_id: BlockId);
    pub fn children_count(&self) -> usize;

    // === 状态访问 ===
    pub fn is_showing(&self) -> bool { self.is_visible && self.is_enabled }
    pub fn set_visible(&mut self, visible: bool) { self.is_visible = visible; }
    pub fn set_enabled(&mut self, enabled: bool) { self.is_enabled = enabled; }

    // === 几何委托 ===
    pub fn figure_bounds(&self) -> Rectangle { self.figure.bounds() }
    pub fn contains_point(&self, x: f64, y: f64) -> bool { self.figure.contains_point(x, y) }
    pub fn intersects(&self, rect: Rectangle) -> bool { self.figure.intersects(rect) }
    pub fn client_area(&self) -> Rectangle { self.figure.client_area() }
    pub fn insets(&self) -> (f64, f64, f64, f64) { self.figure.insets() }

    // === 布局访问 ===
    pub fn get_preferred_size(&self) -> (f64, f64) {
        if let Some(size) = self.preferred_size {
            return size;
        }
        if let Some(ref lm) = self.layout_manager {
            return lm.get_preferred_size(self.id, ...);
        }
        self.figure_bounds().size()
    }
}
```

## FigureGraph 职责设计

### 核心职责

```rust
pub struct FigureGraph {
    // === 节点存储（SlotMap 实现 ID 引用）===
    nodes: SlotMap<BlockId, FigureBlock>,
    uuid_map: HashMap<Uuid, BlockId>,

    // === 根节点 ===
    root: BlockId,
    contents: Option<BlockId>,

    // === 全局布局 ===
    layout_manager: Option<Arc<dyn LayoutManager>>,

    // === 更新调度 ===
    update_manager: Arc<dyn UpdateManager>,

    // === 布局缓存 ===
    constraints: HashMap<BlockId, Box<dyn Constraint>>,
    layout_valid: bool,
}
```

### 树遍历（避免递归）

```rust
impl FigureGraph {
    /// 迭代遍历所有子节点（避免递归栈溢出）
    pub fn iter_subtree(&self, root_id: BlockId) -> FigureBlockIter {
        FigureBlockIter::new(self, root_id)
    }

    /// 迭代查找命中节点
    pub fn hit_test(&self, x: f64, y: f64) -> Option<BlockId> {
        // 从 contents 开始，反向遍历 children
        let contents = self.contents?;
        self.iter_children_reverse(contents)
            .find(|&id| self.blocks[id].contains_point(x, y))
    }

    /// 迭代渲染遍历
    pub fn render_iterative(&self, root_id: BlockId, gc: &mut NdCanvas) {
        let mut stack = vec![root_id];
        while let Some(id) = stack.pop() {
            let block = &self.blocks[id];
            if !block.is_visible { continue; }

            gc.push_state();
            block.figure.paint_figure(gc);
            gc.pop_state();

            // 子节点入栈（正序遍历）
            for &child_id in &block.children {
                stack.push(child_id);
            }
        }
    }
}
```

## Trait 之间的依赖关系

### 依赖关系图

```text
dyn Figure
  │
  │ 不持有 FigureGraph 或 LayoutManager
  │ 只提供 bounds() / paint_figure() / validate()
  │
  ▼
FigureGraph
  │
  │ 持有 SlotMap<BlockId, FigureBlock>
  │
  ├──► FigureBlock.figure: Box<dyn Figure>
  │
  ├──► layout_manager: Arc<dyn LayoutManager>
  │     │
  │     └──► LayoutManager 需要通过 BlockId 查询 FigureGraph
  │         fn layout(container_id: BlockId, graph: &FigureGraph)
  │
  └──► update_manager: Arc<dyn UpdateManager>
        │
        └──► UpdateManager 需要修改 FigureGraph 状态
            fn perform_update(graph: &mut FigureGraph, canvas: &mut NdCanvas)
```

### 避免循环依赖

关键设计：**Figure Trait 不持有 FigureGraph 或 LayoutManager**。

```rust
// 错误示例（循环依赖）
trait Figure {
    fn do_something(&self, graph: &FigureGraph);  // ✗
}

// 正确示例（通过 ID 查询）
trait LayoutManager {
    fn layout(&self, container_id: BlockId, graph: &mut FigureGraph);
    // LayoutManager 通过 BlockId 查询 FigureGraph 获取节点数据
    // FigureGraph 通过 Arc<dyn LayoutManager> 调用 layout()
}
```

## 与当前实现对比

| 组件 | 当前实现 | 理想设计 | 变化 |
|---|---|---|---|
| `FigureBlock` | 已有，职责混合 | 保持，更清晰的内聚边界 | 移除 `is_coordinate_root`（已做） |
| `dyn Figure` | `Bounded → Figure → Shape` 三层 | 保持 `Figure → Shape` | `Bounded` 中的布局方法移除 |
| `Bounded` trait | 包含 `preferred/minimum/maximum_size` | 这些属于布局，应在 FigureBlock 中 | 待迁移 |
| `FigureGraph` | 已有 | 保持，补充文档强调 SlotMap 核心作用 | 名称已更新 |
| `LayoutManager` | 已有 trait | 保持，纯接口设计 | 接收 BlockId 参数 |
| `UpdateManager` | 已有 `SceneUpdateManager` | 保持，两阶段更新 | 接收 BlockId 参数 |
| `SceneHost` | 持有 `FigureGraph` | 映射 draw2d `LightweightSystem` | 对应关系明确 |

## 重构优先级

### P0：立即可做

1. **移除 `is_coordinate_root`** - 已完成（死代码）
2. **`preferred_size/minimum_size/maximum_size` 从 `Bounded` trait 移除**
   - 这些属于布局信息，应在 FigureBlock 中通过 LayoutManager 计算
   - 保留 `figure.bounds()` 作为几何定义

### P1：短期

3. **FigureBlock 方法重分类**
   - 几何委托方法 → 保持 `figure_bounds()`, `contains_point()` 等
   - 布局访问方法 → 委托 `layout_manager`
   - 树管理方法 → `add_child()`, `remove_child()`

4. **Figure trait 文档完善**
   - 明确 Figure 是无状态的，只有几何 + 渲染
   - 运行时状态全部在 FigureBlock 中

### P2：中期

5. **LayoutManager trait 接收 BlockId**
   - 从 `fn layout(&self, container: &dyn Figure)` 改为 `fn layout(&self, container_id: BlockId, graph: &FigureGraph)`
   - 通过 ID 查询，不持有引用

6. **UpdateManager trait 接收 BlockId**
   - 从持有 `IFigure root` 改为持有 `BlockId`
   - 通过 ID 查询

### P3：长期

7. **SceneHost 与 FigureGraph 解耦**
   - SceneHost 只持有 `Arc<FigureGraph>`
   - UpdateManager 通过 Arc 接口操作

## 文档映射

| draw2d 文档 | Novadraw 对应 |
|---|---|
| `IFigure.java` (~1050 行) | `dyn Figure` trait |
| `Figure.java` (~2400 行) | `FigureBlock` (状态) + `dyn Figure` (行为) |
| `LightweightSystem.java` (~611 行) | `SceneHost` |
| `UpdateManager.java` (~200 行) | `dyn UpdateManager` |
| `DeferredUpdateManager.java` (~368 行) | `SceneUpdateManager` |
| `LayoutManager.java` (~80 行) | `dyn LayoutManager` |
| `Figure.paint()` 模板方法 | `FigureGraph` 遍历调度 |

## 参考资料

- draw2d 源码：`/Users/bytedance/Documents/code/GitHub/gef-classic/org.eclipse.draw2d/src/org/eclipse/draw2d/`
- IFigure: `IFigure.java` (~1050 行)
- Figure: `Figure.java` (~2388 行)
- LayoutManager: `LayoutManager.java` (~80 行)
- UpdateManager: `UpdateManager.java` (~200 行)
- DeferredUpdateManager: `DeferredUpdateManager.java` (~368 行)
- LightweightSystem: `LightweightSystem.java` (~611 行)
